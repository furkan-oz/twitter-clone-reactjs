import './ShowMore.css'

const ShowMore = (props) => {
    return(
        <div style={{borderRadius: props.borderRadius}} className="show-more p-3">
            Show more
        </div>
    )
}

export default ShowMore;